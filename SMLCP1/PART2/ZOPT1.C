#include <stdio.h>
#include <string.h>
#include "zopt.h"

pass1()
{
    int i;
    int saved[5];
    char *temp;
    char *tail,*tail2;
    char *last,*this,*next;

    switch_down(1)
    saved[0] = saved[1] = saved[2] = saved[3] = saved[4] =0

    last = alloc(LINELEN);
    this = alloc(LINELEN);
    next = alloc(LINELEN);

    getline(last);
    getline(this);

    

}